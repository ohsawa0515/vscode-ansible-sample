name: "Ansible Lint"

on:
  pull_request:
    types: [synchronize, opened]

jobs:
  ansible-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r .devcontainer/requirements.txt

      - name: Add problem matcher
        run: echo "::add-matcher::.github/ansible-lint.json"

      - name: Run ansible-lint
        run: |
          ansible-lint --offline -p playbooks
      # - name: Lint Ansible Playbooks
      #   uses: reviewdog/action-ansiblelint@8bb20b42133697620fab9651f44661a24923308a # v1.4.0
      #   with:
      #     github_token: ${{ secrets.github_token }}
      #     ansiblelint_version: 5.0.7
      #     reporter: github-pr-review # Change reporter.
      #     ansiblelint_flags: "playbooks/*"
